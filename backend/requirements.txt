# Runtime and tooling dependencies are added in the phase that first uses them,
# so every entry has a visible reason. See CLAUDE.md section 4 for the approved stack.

# --- Phase 1: provider layer ---
httpx==0.28.1
pydantic==2.13.4
pydantic-settings==2.15.0
tenacity==9.1.4

# --- Tooling ---
pytest==8.3.3
pytest-asyncio==0.24.0
respx==0.23.1
ruff==0.7.4
mypy==1.13.0
